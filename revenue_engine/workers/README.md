# Workers

Planned production workers: lead imports, outreach dispatch, AI classification/drafting, payment reconciliation and Telegram notifications. Use Redis-backed queues, retries with exponential backoff, dead-letter handling, job IDs and idempotency keys.
